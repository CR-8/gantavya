import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['framerusercontent.com', 'images.unsplash.com', 'source.unsplash.com'],
  },
};

export default nextConfig;
